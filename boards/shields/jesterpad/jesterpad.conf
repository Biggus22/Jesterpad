# Enable RGB underglow
CONFIG_ZMK_RGB_UNDERGLOW=y

# Enable SPI for LEDs
CONFIG_SPI=y

# Optional: Set default brightness (0-100)
CONFIG_ZMK_RGB_UNDERGLOW_BRT_START=50

# On start, turn on RGB
CONFIG_ZMK_RGB_UNDERGLOW_ON_START=y